node_modules/
dist/
.env
*.log
.DS_Store
src/generated/
.whatsapp-sessions/
uploads/
frontend/test-results/

# Artefatos de teste/documentacao avulsa - nao versionados por ora
whatsapp-qr.png
files.zip
gestaodevendas_documentacao.docx

# Sobras de testes manuais (token/sessao) - nunca versionar
.token-temp.txt
.session-id-temp.txt

# Artefatos compilados do scripts/ (fonte e o .ts)
scripts/*.js
scripts/*.js.map
scripts/*.d.ts

# Projeto open source de referencia (analisado so para ideias de
# reaproveitamento, nao faz parte do backend - ver CLAUDE.md)
WACRM/

# Imagens de design brutas na raiz - nao usadas pelo codigo (o app usa
# so um wordmark em texto, "gestordevendas" - nao ha logo em imagem
# processada em frontend/public/ hoje)
gestor-de-vendas-fundo-*.png
ivillar-logo-fundo-*.png

# Configuracao local do Claude Code (nao parte do produto)
.claude/
